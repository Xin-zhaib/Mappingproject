<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UI.LianxiActivity"></activity>
        <activity android:name=".UI.DaohangActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UI.ShijuanActivity" />
        <activity android:name=".UI.XueshengActivity" />
        <activity android:name=".UI.ChengjiActivity" />
        <activity android:name=".UI.TimuActivity" />
        <activity android:name=".UI.AddActivity" />
        <activity android:name=".UI.DengluActivity"/>
        <activity android:name=".UI.MainActivity" />

    </application>

</manifest>